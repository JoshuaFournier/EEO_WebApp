@model IEnumerable<Predix.Models.Oeedowntime>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DateDebut)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DateFin)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Duree)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Commentaire)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Equipement)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateDebut)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateFin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duree)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Commentaire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Equipement.EquipementId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OeedowntimeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OeedowntimeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OeedowntimeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
